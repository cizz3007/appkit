- 슬롯(slot)은 부모 컴포넌트가 자식 컴포넌트에 콘텐츠(HTML 요소, 텍스트 등)를 전달할 수 있게 해주는 자리 표시자입니다. <slot></slot>이 있는 위치에 부모가 넣은 내용이 삽입됩니다.

```html
<wui-card>
  <p>이 내용이 slot으로 들어갑니다.</p>
</wui-card>
```

- react의 children 이라고 생각하십쇼
